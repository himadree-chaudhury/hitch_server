enum Provider {
  CREDENTIALS
  GOOGLE
}

enum UserRole {
  USER
  HOST
  ADMIN
}

enum UserStatus {
  ACTIVE
  BLOCKED
  BANNED
}

enum HostStatus {
  PENDING
  APPROVED
  REJECTED
}

enum Gender {
  MALE
  FEMALE
  OTHER
}

enum EventType {
  ONLINE
  OFFLINE
  HYBRID
}

enum EventStatus {
  UPCOMING
  ONGOING
  COMPLETED
  CANCELLED
}

enum ParticipantStatus {
  PENDING_PAYMENT
  PAID
  CANCELLED
  WAITLISTED
}

enum PaymentStatus {
  UNPAID
  UNCONFIRMED
  SUCCEEDED
  CANCELLED
  FAILED
}
